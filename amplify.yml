version: 1
applications:
  - appRoot: apps/web
    frontend:
      # Run commands from repo root so npm workspace scripts resolve correctly.
      buildPath: /
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - rm -f apps/web/.env.production
            - printf 'NEST_INTERNAL_URL=%s\n' "${NEST_INTERNAL_URL:-}" >> apps/web/.env.production
            - printf 'NEST_INTERNAL_URLS=%s\n' "${NEST_INTERNAL_URLS:-}" >> apps/web/.env.production
            - printf 'CLIENT_API_URL=%s\n' "${CLIENT_API_URL:-}" >> apps/web/.env.production
            - printf 'APP_BASE_URL=%s\n' "${APP_BASE_URL:-}" >> apps/web/.env.production
            - printf 'WEB_APP_URL=%s\n' "${WEB_APP_URL:-}" >> apps/web/.env.production
            - npm run build --workspace web
            # Install runtime dependencies for the SSR compute bundle.
            - cd apps/web/build/compute/default
            - npm i --omit=dev
      artifacts:
        baseDirectory: apps/web/build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
