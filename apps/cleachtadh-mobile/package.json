{
  "name": "cleachtadh-mobile",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "web:build:bundled": "npm run build:mobile --workspace cleachtadh-web",
    "cap:sync": "npx cap sync",
    "cap:sync:bundled": "npm run web:build:bundled && npx cap sync",
    "cap:add:ios": "npx cap add ios",
    "cap:add:android": "npx cap add android",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android",
    "cap:run:ios": "npm run cap:sync:bundled && npx cap run ios --no-sync",
    "cap:run:android": "npm run cap:sync:bundled && npx cap run android --no-sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/app": "^7.0.2",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0"
  }
}
